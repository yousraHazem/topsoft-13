    <meta http-equiv="Content-Type" content="text/html;charset=UTF-8">
    <table>
      <tr>
        <td>
          <%= form_for(:project, :url =>  {:action => 'listProjects', :class => 'back link'}) do |f| %>
            <ul class="nav nav-list">
              <button type="submit"><i class=" icon-th-list"></i>قائمة المشاريع</button>
           </ul>
           <% end %>
        </td>
        <td>
          <%= form_for(:project, :url => {:controller => 'budget_items',:action => 'list', :id => @project.id}) do |f| %>
            <ul class="nav nav-list">
              <button type="submit"><i class="icon-tasks"></i>ميزانية المشروع</button>
           </ul>
           <% end %>
        </td>
        <td>
          <%= form_for(:project, :url => {:action => 'editProject',:id=> @project_id}) do |f| %>
           <ul class="nav nav-list">
              <button type="submit"><i class="icon-edit"></i>تعديل المشروع</button>
           </ul>
          <% end %>
        </td>
        <td>
           <%= form_for(:project, :url =>  {:controller =>'tasks', :action =>'view',:id=> @project_id}) do |f| %>
            <ul class="nav nav-list">
              <button type="submit"><i class="icon-tasks"></i>المهمات</button>
           </ul>
           <% end %>
        </td>
      </tr>
    </table>



<div class="aligncenter">
<h1><%= @project.name %></h1> 
</div>    
<div class="alignright">
  <table class="table table-hover" align="right">
  	<tr>      
      <ul class="nav nav-list">
      <td><i class="icon-th"></i> تاريخ البداية</td>
      </ul>
   		<td><%= @project.start_date %></td>
 	  </tr> 	
 	  <tr >
      <ul class="nav nav-list">
      <td><i class="icon-th"></i> تاريخ النهاية</td>
      </ul>
   		<td><%= @project.start_date %></td>
 	  </tr>
  	<tr >
      <ul class="nav nav-list">
      <td><i class="icon-pencil"></i>تفاصيل المشروع</td>
      </ul>
      <td><%= @project.description %></td>
    </tr>
 	</table>
</div>


