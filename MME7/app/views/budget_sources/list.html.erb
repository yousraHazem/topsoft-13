<% @page_title = "Budget_Source List"%>



<div>
	<h2>مصدر الميزاني</h2>
    
	<%= link_to({:action=>'new'}, class: "btn btn-success") do %>
       <i class="icon-plus icon-white"></i>
      <% end %>


 <table>
<tr>
	<th>إسم</th>
	<th>كمية</th>
	<th>مشروع</th>
	<th>الكمية للمشروع</th>
	<th>فعل</th>
	
	
</tr>
<% @budget_sources.each do |budget_source|%>
<tr>
	<td><%= budget_source.budget_source_name %></td>	
<td><%= budget_source.amount %></td>
<td><div><% budget_source.budget_source_projects.each do |budget_source| %>
 <%= budget_source.project.name%> <%= budget_source.amount%> </td>
		
<%end%>

</div>
<td><%= link_to("تعديل", {:action => 'edit', :id => budget_source.id}, :popup => true) %></td>
<td> <%= link_to "إلغاء", {:action => 'destroy', :id => budget_source.id},:confirm => "Are you sure you want to delete this budget source?" %></td>
<td><%= link_to("موساهمات للمشاريع", {:controller => 'budget_source_projects' , :action => 'addamount', :id => budget_source.id}) %></td>
</tr>
<%end%>
</table>

</div>