<%= render(:partial => 'shared/error_messages' , :locals => {:object => @budget_item}) %>
<div> 
<h2>تعديل عنصر الميزنية</h2>
<%= form_for(:budget_item, :url=>{:action=>'update' , :item => @budget_item.id , :id => @project}) do |f| %>
<table>
	<tr>
		<th><%= f.label(:Name ,"إسم") %></th>
			<td><%= f.text_field(:name)%></td>
	</tr>
</br>
	<tr>
		<th><%= f.label(:Operational ,"تشغيل")%></th>
			<td><%= f.check_box(:operational)%></td>
	</tr>
</br>
</table>
</br>
	<p>
		<td><%= f.submit ("تسجيل") %></td>
	</p>
<% end %>
</div>

