<meta http-equiv="Content-Type" content="text/html;charset=UTF-8">
<body dir="rtl" lang="ar">
<% if @items.blank? %>
<% else %>
<table class="table">
 	<tr class="header">
 		<th>اسم عنصر الميزانية</th>
 		<th>مجموع</th>
 		<th>تم صرفه</th>
 		<% if loged_in? %>
 		<% if isProjectCreator(@current_user,@project.id)%>
        <th>تعديل الميزانية</th>
        <th>اعضاء العنصر</th>
        <th>اعضاء العنصر</th>
 		<% end %>
 		<% end %>
 		<th>مشاهدة</th>
 	</tr>
 	<% @items.each do |item| %>
		<tr id = "budget_item">
 		<td><%= item.name %></td>
 		<td><%= item.total %></td>
 		<td><%= item.spent %></td>
 		<% if loged_in? %>
 		<% if isProjectCreator(@current_user,@project.id)%>
        <td class="actions">
		<%= link_to("تعديل", {:action=>'edit' , :item=>item.id , :id => item.project_id} ,:class=>"btn btn-small")%>
	    </td>
	    <td class="actions">
	    <%= link_to("الاعضاء", {:controller =>'budget_items', :action =>'getProjectMembers',:project_id => @project.id , :budget_item_id => item.id }, :class => 'btn btn-small') %>
		</td>
	    <% end %> 
	    <% end %>
	    <td><%= link_to("مشاهدة", {:controller=>'budget_components',:action=>'list',:id=>item.id ,:project_id=> @project.id}, :class=>"btn btn-small")%></td>
 		</tr>
 	<% end %>
</table>
<% end %>
</body>

