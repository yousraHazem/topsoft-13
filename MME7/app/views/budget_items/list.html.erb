
<% @page_title="Budget Item"%><%= link_to("Back to project", {:controller=>'projects',:action => 'show', :id=>params[:project_id],:class => 'back link'} ) %>

<form action="">
<fieldset>
<legend><h2>Budget of Project: <%= @project_name.name %></h2></legend>
<h3>Total Budget: <%= @total_budget %></h3> 
<h6><i class="icon-tint"></i>Total Spent: <%= @total_spent %></h6>

<h6><i class="icon-tint"></i>Total Raised: <%= @raised %></h6>
</fieldset>
</form>

<h4> <%= link_to("#",:id=>"new-item" , class: "btn btn-success") do %>
    <i class="icon-plus icon-white"></i> 
      <% end %>  Items</h4>
 <% if @items.blank? %>
<p>There are no current budget items </p>
<% end %>

<div id="item-form" style="display:none;" >
<%= form_for(:budget_item, :url=>{:action=>'create' , :project_id => params[:project_id] }) do |f| %>
	<%= render(:partial=>'budget', :locals=>{:f=>f}) %>
	<td><%= f.submit %></td>
<% end %>

</div>
</br></br>
 <table class="table" summary="Subject List">
<tr class="header">
	<th>Name</th>
	<th>Action</th>
</tr>
<% @items.each do |item|%>

<tr>
	<td><%= item.name %></td>

	<td class="actions">
		<%= link_to("Edit", {:action=>'edit' ,:id=>item.id , :project_id => item.project_id},:class=>"btn btn-small")%>
		<%= link_to("View", {:controller=>'budget_components',:action=>'list',:id=>item.id ,:project_id=> item.project_id,:name=>item.name}, :class=>"btn btn-small")%>	
	</td>
	
</tr>
<%end%>


