
 <div class="navbar navbar-fixed-top">

      <div class="container">
        <a class="brand">Groups List</a>
    </div>
 </div>

<div><%= link_to("Add New Group", {:action => 'new'}, :class => 'container') %></div>


<table>
  <tr>
    <th>Name</th>
  </tr>

  <% if @groups %>
  <% @groups.each do |group| %>
  <tr>
    <td class="gg"><%= group.group_name %></td>
    <td><%= link_to("Show", {:action => 'show', :id => group.id}, :class => 'span1') %></td>
    <td><%= link_to("Delete", {:action => 'delete', :id => group.id}, :class => 'span1') %></td>
  </tr>
<% end %>
</table>


<br />

<% end %>
