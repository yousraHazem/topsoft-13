
	<%= form_for(:comment, :url=>{:action=>'updateCommentGroup', :id=> params[:id], :post_id => params[:post_id], :group_id => @group.id}) do |f| %>	

	<table summary = "The Comment">
		
		<tr>
			<th><%= f.label(:comment, "Comment")%></th>
			<td><%= f.text_field(:comment) %></td>
       </tr>

	</table>

	<% end %>

</div>