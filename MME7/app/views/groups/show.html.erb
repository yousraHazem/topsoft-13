<%= link_to("<< Back to groups list", {:action => 'index'}, :class => 'back-link') %>


<div>
   
  <table>
   <tr >
    <th>Group name</th>
    <td><%= @group.group_name %></td>
   </tr>

   <tr >
    <th>Description</th>
    <td><%= @group.description %></td>
   </tr>
   
  </table>

     <div>
 <% if ifMember(params[:id],6)==false %>

 <%= button_to("join",{:controller => groupusers,:action =>'join', :id => params[:id]}, :class => 'action show') %>
                   
 <% end %>

    <% if ifMember(params[:id],6)==true %>
 <%= link_to("create post", {:controller => 'posts', :action => 'new', :id =>@group_id}, :class => 'action new') %>      
 <% end %>
     </div>