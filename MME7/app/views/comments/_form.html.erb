<div class="comment new">

<%= form_for(:comment, :url => {:action => 'createComment'}) do |f| %>
  <div class="field">

    <%= f.text_field :comment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>